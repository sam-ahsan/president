{
  "name": "@president/realtime",
  "version": "1.0.0",
  "description": "Realtime worker for President card game",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "build": "tsc && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@president/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.19.0"
  }
}

