{
  "name": "president-card-game",
  "version": "1.0.0",
  "description": "A real-time multiplayer President card game built with Cloudflare Workers and React",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter @president/api dev\" \"pnpm --filter @president/realtime dev\" \"pnpm --filter web dev\"",
    "build": "pnpm --filter shared build && pnpm --filter api build && pnpm --filter realtime build && pnpm --filter web build",
    "deploy": "pnpm build && wrangler deploy",
    "deploy:api": "pnpm --filter api deploy",
    "deploy:realtime": "pnpm --filter realtime deploy",
    "deploy:web": "pnpm --filter web deploy",
    "db:migrate": "wrangler d1 migrations apply president-db",
    "db:create": "wrangler d1 create president-db",
    "type-check": "pnpm --filter shared type-check && pnpm --filter api type-check && pnpm --filter realtime type-check && pnpm --filter web type-check"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "concurrently": "^8.2.2",
    "typescript": "^5.3.0",
    "wrangler": "^3.19.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}

